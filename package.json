{
  "name": "django-real-estate-app",
  "version": "1.0.1",
  "description": "Django real estate app",
  "private": true,
  "scripts": {
    "heroku-prebuild": "npm i --prefix client && npm install && npm run build && cd .."
  },
  "engines": {
    "node": "14.15.4",
    "npm": "6.14.10"
  }
}